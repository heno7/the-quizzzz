const path = require("path");

module.exports = {
  config: path.posix.resolve("src/databases/config", "config.js"),
  "models-path": path.posix.resolve("src/databases", "models"),
  "seeders-path": path.posix.resolve("src/databases", "seeders"),
  "migrations-path": path.posix.resolve("src/databases", "migrations"),
};
